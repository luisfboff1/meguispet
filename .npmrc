# Configurações otimizadas do npm
cache-min=86400
prefer-offline=true
audit-level=moderate
fund=false

# Configurações de segurança
engine-strict=true
save-exact=true

# Configurações de performance
maxsockets=15
registry=https://registry.npmjs.org/
