# Visual Guide: Stock Validation Error Fix

## 🔴 Problem Flow (Before Migration 015)

```
┌─────────────────────────────────────────────────────────────────────┐
│ USER ACTION: Create Sale                                            │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Frontend: VendaForm Component                                       │
│ - User adds Product 5 (quantity: 1) to cart                        │
│ - User clicks "Finalizar Venda"                                    │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ API Route: POST /api/vendas                                         │
│ - Receives sale data                                                │
│ - Line 130: Calls validateStockAvailability()                      │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Stock Manager: validateStockAvailability()                          │
│ - Line 143: Calls get_stock_with_lock(produto_id=5, estoque_id=1) │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Database Function: get_stock_with_lock()                            │
│ - Tries to query: SELECT * FROM produtos_estoques WHERE ...        │
│ - 🔒 RLS CHECK: Can user access produtos_estoques?                 │
│ - ❌ NO RLS POLICY EXISTS!                                         │
│ - Returns: NULL (no data)                                          │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Stock Manager: validateStockAvailability()                          │
│ - Receives: NULL or 0 stock                                        │
│ - Actual stock in DB: 15367 units                                  │
│ - But function can't see it due to RLS!                            │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ API Route: POST /api/vendas                                         │
│ - Validation fails: stockData.quantidade (0) < item.quantidade (1) │
│ - Returns 400 error                                                 │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Frontend: Error Message                                             │
│ ❌ Estoque insuficiente para os seguintes produtos:                │
│    Product 5 (disponível: 0, solicitado: 1)                        │
└─────────────────────────────────────────────────────────────────────┘
```

---

## ✅ Solution Flow (After Migration 015)

```
┌─────────────────────────────────────────────────────────────────────┐
│ USER ACTION: Create Sale                                            │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Frontend: VendaForm Component                                       │
│ - User adds Product 5 (quantity: 1) to cart                        │
│ - User clicks "Finalizar Venda"                                    │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ API Route: POST /api/vendas                                         │
│ - Receives sale data                                                │
│ - Line 130: Calls validateStockAvailability()                      │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Stock Manager: validateStockAvailability()                          │
│ - Line 143: Calls get_stock_with_lock(produto_id=5, estoque_id=1) │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Database Function: get_stock_with_lock()                            │
│ - 🔐 SECURITY DEFINER: Function runs with elevated privileges      │
│ - ✅ RLS CHECK BYPASSED: Function can access all data              │
│ - BUT: User auth still validated (auth.uid() IS NOT NULL)          │
│ - Queries: SELECT * FROM produtos_estoques WHERE ...               │
│ - Returns: { quantidade: 15367, produto_nome: "Product 5" }        │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Stock Manager: validateStockAvailability()                          │
│ - Receives: stockData.quantidade = 15367                           │
│ - Validation: 15367 >= 1 ✅                                        │
│ - Returns: { valid: true, insufficientStock: [] }                  │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ API Route: POST /api/vendas                                         │
│ - Validation passes ✅                                              │
│ - Creates sale record                                               │
│ - Updates stock: 15367 - 1 = 15366                                 │
│ - Returns 201 success                                               │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Frontend: Success Message                                           │
│ ✅ Venda realizada com sucesso! Estoque atualizado.                │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔐 Security Model Comparison

### Before Migration 015

```
┌─────────────────────────────────────────┐
│ Table: produtos_estoques                │
│ RLS: ❌ No policies defined             │
│                                         │
│ Authenticated User Tries to Query:     │
│ SELECT * FROM produtos_estoques         │
│                                         │
│ Result: ❌ No data returned             │
│ (RLS blocks access by default)         │
└─────────────────────────────────────────┘
```

### After Migration 015

```
┌────────────────────────────────────────────────────────────────┐
│ Table: produtos_estoques                                       │
│ RLS: ✅ Enabled with policies                                  │
│                                                                │
│ Direct Query (via Supabase Client):                           │
│   ┌──────────────────────────────────────────────────┐        │
│   │ Policy: "Authenticated users view product stock" │        │
│   │ Condition: auth.uid() IS NOT NULL                │        │
│   │ Result: ✅ Data returned if authenticated        │        │
│   └──────────────────────────────────────────────────┘        │
│                                                                │
│ Via Function (get_stock_with_lock):                           │
│   ┌──────────────────────────────────────────────────┐        │
│   │ Function: SECURITY DEFINER                       │        │
│   │ - Bypasses RLS (elevated privileges)             │        │
│   │ - BUT validates auth.uid() IS NOT NULL           │        │
│   │ - Sets search_path = public (security)           │        │
│   │ Result: ✅ Data returned if authenticated        │        │
│   └──────────────────────────────────────────────────┘        │
└────────────────────────────────────────────────────────────────┘
```

---

## 📊 Database Changes

### Tables with New RLS Policies

```
┌─────────────────────────────────────────────────────────────────────┐
│ Table: estoques                                                     │
│ ├─ Policy: Authenticated users view stock locations                │
│ ├─ Policy: Authenticated users insert stock locations              │
│ ├─ Policy: Authenticated users update stock locations              │
│ └─ Policy: Admins delete stock locations                           │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│ Table: produtos_estoques                                            │
│ ├─ Policy: Authenticated users view product stock                  │
│ ├─ Policy: Authenticated users insert product stock                │
│ ├─ Policy: Authenticated users update product stock                │
│ └─ Policy: Admins delete product stock                             │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│ Table: estoques_historico                                           │
│ ├─ Policy: Authenticated users view stock history                  │
│ ├─ Policy: Authenticated users insert stock history                │
│ ├─ Policy: Stock history is immutable (UPDATE = false)             │
│ └─ Policy: Admins delete stock history                             │
└─────────────────────────────────────────────────────────────────────┘
```

### Functions with Updated Security

```
┌─────────────────────────────────────────────────────────────────────┐
│ Function: get_stock_with_lock                                       │
│ Before: SECURITY INVOKER (default)                                 │
│ After:  SECURITY DEFINER + SET search_path = public                │
│ Purpose: Read stock with row locking                               │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│ Function: adjust_stock_with_lock                                    │
│ Before: SECURITY INVOKER (default)                                 │
│ After:  SECURITY DEFINER + SET search_path = public                │
│ Purpose: Update stock atomically with locking                      │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│ Function: adjust_bulk_stock_with_lock                               │
│ Before: SECURITY INVOKER (default)                                 │
│ After:  SECURITY DEFINER + SET search_path = public                │
│ Purpose: Update multiple products in one transaction               │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│ Function: get_stock_history                                         │
│ Before: SECURITY INVOKER (default)                                 │
│ After:  SECURITY DEFINER + SET search_path = public                │
│ Purpose: Retrieve stock movement audit trail                       │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🎯 Why This Fix Works

### The Problem
```
User (authenticated) → Function → Query Table → RLS Check
                                                    ↓
                                            ❌ No policy
                                                    ↓
                                            Returns NULL
```

### The Solution
```
User (authenticated) → Function (SECURITY DEFINER) → Query Table
                            ↓                              ↓
                    Checks auth.uid()              Bypasses RLS
                            ↓                              ↓
                      ✅ Validated                  ✅ Data returned
```

### Why It's Secure
```
┌─────────────────────────────────────────────────────────────────┐
│ Security Layers:                                                │
│                                                                 │
│ 1. API Authentication                                           │
│    ├─ Middleware: withSupabaseAuth()                           │
│    └─ Validates JWT token                                      │
│                                                                 │
│ 2. Function-Level Auth Check                                   │
│    ├─ Every function checks: auth.uid() IS NOT NULL            │
│    └─ Only authenticated users can call functions              │
│                                                                 │
│ 3. Table-Level RLS (for direct access)                         │
│    ├─ Policies protect direct table queries                    │
│    └─ Prevents unauthorized API/SQL access                     │
│                                                                 │
│ 4. SQL Injection Protection                                     │
│    ├─ SET search_path = public                                 │
│    └─ Prevents schema manipulation attacks                     │
│                                                                 │
│ 5. Business Logic Validation                                    │
│    ├─ Stock can't go negative                                  │
│    ├─ Quantities must be positive                              │
│    └─ All changes logged in history table                      │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📈 Expected Impact

### Before Migration 015
```
Sales Success Rate: 0% ❌
Error Rate: 100% 🔴
User Experience: Broken 💔
```

### After Migration 015
```
Sales Success Rate: 100% ✅
Error Rate: 0% 🟢
User Experience: Working 🎉
```

---

## 📝 Quick Reference

### Apply Migration
```bash
supabase db push
```

### Verify Success
```sql
-- Should return 'true' for all tables
SELECT tablename, rowsecurity 
FROM pg_tables 
WHERE tablename IN ('estoques', 'produtos_estoques', 'estoques_historico');
```

### Test Stock Access
```sql
-- Replace with real IDs
SELECT * FROM get_stock_with_lock(1, 1);
```

---

**For detailed instructions, see:** `FIX_SUMMARY.md`  
**For comprehensive docs, see:** `database/migrations/README_015.md`  
**For verification tests, see:** `database/migrations/test_015.sql`
